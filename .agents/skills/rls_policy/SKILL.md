---
name: rls_policy
description: Padrões canônicos, checklist de segurança e regras de governança para Row Level Security (RLS) no banco de dados Supabase/PostgreSQL. Gatilhar sempre que for desenhar esquemas SQL, migrations, políticas ou consultas de dados de usuários.
---

# Skill: RLS Policy — Best Practices

**This is a pointer, not the canonical body.** The full skill content lives in
[`docs/skills/rls_policy.md`](../../../docs/skills/rls_policy.md) — that is the file
`agentes/02_Worker_TRM_Agent.md` §2.1 actually instructs the Worker to read.

This stub exists only so Antigravity 2.0's directory-based skill auto-discovery still finds a
`SKILL.md` under `.agents/skills/`. Read `docs/skills/rls_policy.md` in full before writing any
RLS policy or query involving user data — do not treat the frontmatter above as a substitute for
the real content.

*Consolidated from a full duplicate copy — see `docs/26_Version_Unification_Plan.md` T3.3.*
